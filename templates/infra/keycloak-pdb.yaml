apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: keycloak-pdb
  namespace: {{ .Values.global.namespace }}
spec:
  minAvailable: {{ .Values.keycloak.pdb.minAvailable }}
  selector:
    matchLabels:
      app: {{ .Values.keycloak.app.name }}