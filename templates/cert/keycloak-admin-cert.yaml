apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-admin-cert
  namespace: keycloak
spec:
  secretName: keycloak-admin-tls
  issuerRef:
    name: self-signed-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - admin.auth.localhost